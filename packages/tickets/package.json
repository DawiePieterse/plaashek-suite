{
  "name": "@plaashek/tickets",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Sign and verify tickets — farm ceiling, device floor",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test dist"
  },
  "dependencies": {
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.6.0"
  }
}
