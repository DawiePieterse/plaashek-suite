{
  "name": "@plaashek/sync",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PowerSync connector, workspace-row conflict rules, outbox types — see ADR 0002",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@plaashek/schema": "workspace:*"
  },
  "peerDependencies": {
    "@powersync/web": "^1.39.0"
  },
  "devDependencies": {
    "@powersync/web": "^1.39.0",
    "typescript": "^5.6.0"
  }
}
