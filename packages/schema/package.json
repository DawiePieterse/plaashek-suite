{
  "name": "@plaashek/schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared types and validators — one source of truth",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "drizzle-zod": "^0.5.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.28.0",
    "pg": "^8.13.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.6.0"
  }
}
